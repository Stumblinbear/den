{
	"name": "model-prompts-plugin",
	"private": true,
	"type": "module",
	"description": "Runtime dependency for the model-prompts Claude Code plugin hook. Claude Code installs it with `npm ci --ignore-scripts` when it caches the plugin.",
	"license": "MIT OR Apache-2.0",
	"engines": {
		"node": ">=22.6"
	},
	"scripts": {
		"test": "node --experimental-strip-types --disable-warning=ExperimentalWarning --test \"tests/*.test.mts\""
	},
	"dependencies": {
		"smol-toml": "1.8.0"
	}
}
